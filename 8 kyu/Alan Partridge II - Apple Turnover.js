/* Alan Partridge II - Apple Turnover */

function apple(x){
    if(Math.pow(Number(x),2) > 1000){return `It's hotter than the sun!!`}
    else{return `Help yourself to a honeycomb Yorkie for the glovebox.`}
  }